{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug bee_sim (MSVC)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "environment": [
        { "name": "PATH", "value": "${env:PATH};C:\\vcpkg\\installed\\x64-windows\\bin" }
      ]
    }
  ]
}
